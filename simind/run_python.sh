#!/bin/bash
# This script runs a Python file

# Change to the script’s directory (optional, ensures paths work)
cd "$(dirname "$0")"

# Run your Python file
python initial_simind_pipline_v5_total_loc_extend.py
